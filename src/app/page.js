import DivaApp from "./DivaApp";
export default function Home() {
  return <DivaApp />;
}
```

Commit.

---

**Fichier 4 : `src/app/data.js`**

Add file → Create new file → nom : `src/app/data.js`

Ouvre le fichier **data.js** téléchargé, copie tout (Cmd+A → Cmd+C), colle dans GitHub. Commit.

---

**Fichier 5 : `src/app/DivaApp.js`**

Add file → Create new file → nom : `src/app/DivaApp.js`

Ouvre le fichier **DivaApp.js** téléchargé, copie tout (Cmd+A → Cmd+C), colle dans GitHub. Commit.

---

**Résultat final** — ta structure doit être :
```
TDAH-FR/
├── README.md          ✅ déjà là
├── next.config.js     ✅ déjà là
├── package.json       ✅ déjà là
└── src/app/
    ├── globals.css     → fichier 1
    ├── layout.js       → fichier 2
    ├── page.js         → fichier 3
    ├── data.js         → fichier 4
    └── DivaApp.js      → fichier 5
